# Daml build artifacts (matches at any depth, since every challenge/common
# package has its own main/test subfolders)
.daml/
dist/
*.dar
log/

# OS
.DS_Store

# Local-only notes, not for the repo
CLAUDE_CONTEXT.md
